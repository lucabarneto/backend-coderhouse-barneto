<section>
  <h1>Mostrando todos los productos:</h1>
  <ul>
    {{#each products}}
      <li>
        <div>{{this.title}}</div>
        <div>${{this.price}} - categoría:{{this.category}}</div>
      </li>
    {{/each}}
  </ul>

</section>